# Breakout

Original Atari Breakout recreated in C++.

You must have [SplashKit](https://splashkit.io/) installed.

To build this run:
```bash
cmake --build .
```

Then to run it:
```bash
./breakout
```

Or if on Windows:
```bash
breakout.exe
```
